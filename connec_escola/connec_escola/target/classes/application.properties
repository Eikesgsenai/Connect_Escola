spring.application.name=connec_escola
server.port=8080

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Ativa o console web do H2 para espiar o banco
# Acesse em: http://localhost:8080/h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# 'create-drop' cria o banco do zero quando liga e apaga quando desliga.
# Perfeito para testes.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# --- CONFIGURA��O DE PRODU��O (RENDER/POSTGRES) ---
# ATEN��O: Quando voc� for subir o projeto para o Render,
# voc� vai apagar as linhas do H2 acima e descomentar (#) estas aqui:
#
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASS}
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# --- Configura��o do JWT ---
# Esta � a "senha mestra" para criar os tokens.
# Pode ser qualquer string longa e aleat�ria (m�nimo de 32 caracteres)
jwt.secret=secreta8081050112secreta8081050112secreta8081050112secreta8081050112secreta8081050112secreta8081050112
# --- Configura��o de Encoding (CORRE��O DOS ACENTOS) ---
# For�a o Spring Boot a usar UTF-8 para TODAS as requisi��es (incluindo HTML)
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true